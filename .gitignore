# deps
node_modules/
.pnpm-store/

# build output
dist/
.next/
out/
.turbo/

# env / secrets
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# misc
.DS_Store
coverage/
*.tsbuildinfo

# next.js generated (regenerated on dev/build)
next-env.d.ts

# cursor agents sdk local state (JSONL store / scratch cwd)
.cursor-agents/

# claude code local harness state (locks, scratch) — not product code
.claude/

# raw Google-reviews scrape (operator's local source, contains author names). The ANONYMIZED
# derived dataset lives in packages/seed/data/recovery-cases.json (committed); raw scrape stays local.
le-kyoto-google-reviews.json

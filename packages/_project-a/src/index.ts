// ─────────────────────────────────────────────────────────────────────────────
// PROJECT A — Restaurant truth-sync (service-as-software)  ·  PLACEHOLDER
//
// TODO: AWAITS THE A/B PROJECT DECISION. Do NOT build domain logic here until the
// team picks A. When we do: model the single source of truth (menu/prices/hours/
// dishes) and the external surfaces (Maps/social/delivery/site); add domain agent
// roles (inventory, marketing, presence-sync, verifier, orchestrator) on top of
// @weavehacks/orchestration; the conflict (e.g. Marketing wants to promote a dish
// Inventory knows is sold out) drives the demo. Keep the core domain-agnostic.
// ─────────────────────────────────────────────────────────────────────────────

export const PROJECT_A_DECIDED = false;

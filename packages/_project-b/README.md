# @weavehacks/project-b — PLACEHOLDER (do not build yet)

**Project B: Self-improving agent over a vector DB ("ruvector").** Empty on purpose —
it awaits the A/B project decision (see root `CLAUDE.md`). Before any code, sharpen the
thesis (OPEN DECISION #2): what does a solo agent get *wrong* about improving the vector
DB that a team gets *right*? No domain logic until the team picks B.

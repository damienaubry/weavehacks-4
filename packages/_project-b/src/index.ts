// ─────────────────────────────────────────────────────────────────────────────
// PROJECT B — Self-improving agent over a vector DB ("ruvector")  ·  PLACEHOLDER
//
// TODO: AWAITS THE A/B PROJECT DECISION. Do NOT build domain logic here until the
// team picks B. FIRST sharpen the thesis (OPEN DECISION #2): what exactly does a
// SOLO agent get WRONG about improving the vector DB that a TEAM gets right? If we
// can't answer that with a real conflict + a Weave number, B is weaker than A.
// When we do build: model the store/retrieval quality on top of
// @weavehacks/orchestration; keep the core domain-agnostic.
// ─────────────────────────────────────────────────────────────────────────────

export const PROJECT_B_DECIDED = false;
